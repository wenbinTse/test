<div>
  <p>
    <strong style="font-size: 40px"><%=meeting.name%></strong>
    <%if(meeting.startDate == meeting.endDate) {%>
      <span><%=meeting.startDate%></span>
    <%} else {%>
        <span><%=meeting.startDate%> - <%=meeting.endDate%></span>
    <%}%>
  </p>
  <h2>会议介绍</h2>
  <div style="margin-left: 8px">
      <%-meeting.description%>
  </div>
  <h2>
    注意事项
  </h2>
  <div style="margin-left: 8px">
    <%-meeting.detail%>
  </div>
<div>
